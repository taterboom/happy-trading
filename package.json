{
  "name": "happy-trading",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "test": "pnpm --filter \"./packages/*\" test",
    "build": "pnpm run build:core && pnpm run build:others",
    "build:core": "pnpm --filter \"./packages/core\" build",
    "build:others": "pnpm --filter \"./packages/{plugin-monitor,plugin-storage,utils,plugin-notification}\" build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
